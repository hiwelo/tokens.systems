import styled from 'styled-components';

const BoxWrapper = styled.div`
  display: block;
  padding: 1.6rem;
  background-image: repeating-linear-gradient(
    -45deg,
    transparent,
    transparent 0.3rem,
    var(--color-pink2) 0.3rem,
    var(--color-pink2) 0.4rem
  );
`;

export default BoxWrapper;
