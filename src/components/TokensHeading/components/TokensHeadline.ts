import styled from 'styled-components';

import Heading from '../../Heading';

const TokensHeadline = styled(Heading)`
  margin: 4.8rem auto 0;
  text-shadow: 0.1rem 0.1rem 0 var(--color-white), 0.2rem 0.2rem var(--color-session);
  font-size: var(--font-size-heading-main);
  font-weight: 700;
`;

export default TokensHeadline;
